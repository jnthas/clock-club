name: Render Thumbnail

on:
  push:
    paths:
      - 'shared/**'
  workflow_dispatch:
    inputs:
      tags:
        description: 'Reason for manual execution'

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: "{{defaultContext}}:thumbnails/render"
        file: Dockerfile
        push: false
        load: true
        tags: thumbnail-render:latest
        outputs: type=docker
    - name: Execute
      run: |
          echo "$GITHUB_WORKSPACE"
          ls $GITHUB_WORKSPACE
          docker run -v "$GITHUB_WORKSPACE/clock-club/shared:/app/shared" -v "$GITHUB_WORKSPACE/clock-club/thumbnails:/app/exported" thumbnail-render:latest

